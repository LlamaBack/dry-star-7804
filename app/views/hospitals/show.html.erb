<h1><%= @hospital.name %></h1>
<h2>Doctors</h2>
<ol>
    <% @doctors.each do |doctor| %>
        <li id="<%= doctor.id %>">
            <p>Doctor <%= doctor.name %> has <%= doctor.patients.count %> patients</p>
        </li>
    <% end %>
</ol>